return {
  {
    "OXY2DEV/markview.nvim",
    -- Load after colorscheme so highlight groups resolve correctly (plugin docs).
    lazy = false,
    -- Needs the `markdown` + `markdown_inline` treesitter parsers (see treesitter.lua).
  },
}
